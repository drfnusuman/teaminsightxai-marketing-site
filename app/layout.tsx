import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// --- UPDATED GLOBAL METADATA FOR ORGSIGHTXAI ---
export const metadata: Metadata = {
  title: 'OrgSightXAI: AI-Powered Team and Organizational Insight',
  description: 'OrgSightXAI delivers modular, AI-driven solutions for Team Managers, Admins, and Executives to optimize team performance.',
};
// --------------------------------------------------

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}